# PC 列表页办公端化对齐进展

## 阶段目标

- 把 PC Web 的记录页、人才广场、任务广场从卡片展示页收成更像办公端的列表/工作台页面。
- 保持与 `2026-03-28-h5-pc-ui-spec.md` 中“PC 端偏办公软件、信息密度更高、筛选和处理更高效”的方向一致。
- 每完成一块，都同步更新状态文档，避免文档与代码脱节。

## 当前阶段拆分

- [x] 记录列表页改成办公端结构，突出筛选、总览和行级入口。
- [x] 人才广场改成更偏目录/资源池的桌面工作页，弱化营销式 hero。
- [x] 任务广场改成更偏任务池的桌面工作页，保留筛选与详情，但弱化大卡片展示。
- [x] 整理共享样式，收成统一的 PC 列表页视觉语言。
- [x] 同步根文档与四个子项目 docs 副本。
- [x] 跑 PC 与 H5 构建验证，记录下一阶段建议。

## 并行策略

- 子线程 A：记录列表页结构改造
- 子线程 B：人才广场结构改造
- 子线程 C：任务广场结构改造
- 子线程 D：样式复用和办公端视觉建议
- 子线程 E：文档口径与设计对齐检查
- 主线程：整合、样式落地、文档同步、构建验证

## 阶段结果

- `RecordPage.vue` 已改成摘要条、状态筛选和行式列表结构。
- `TalentMarketPage.vue` 已改成更偏资源目录的桌面工作页，并补上快速筛选。
- `TaskMarketPage.vue` 已改成左侧任务池 + 右侧详情轨的 master-detail 工作台。
- `style.css` 已统一补齐三页的办公端列表页视觉语言与响应式收口。
- 根文档与四个子项目 `docs` 副本已同步。
- 验证命令：`cd frontend && npm run build`，结果通过。

## 下一阶段建议

- 继续推进 PC 详情页与表格式列表页：记录详情、人才详情、后台列表和更强的批量处理页。
