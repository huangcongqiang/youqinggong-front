# `达到上线标准` 主线

## 主线目标

把当前“PC / H5 / 后台 / spring-app 已具备演示与部分正式化能力”的状态，推进到满足根文档中“正式上线可测”冻结条件的状态。

## 范围

- `spring-app` 认证与权限正式化
- 任务 / 聊天 / 协作 / 验收 / 记录的正式主写链补缺
- 上传链、IM callback、附件回写与实时事件正式化
- 财务 / 争议 / 风控 / 组织权限的上线缺口补齐
- 监控、告警、限流、压测、健康检查、回滚与上线验收

## 不在本主线范围

- 全新业务方向
- 视觉大改版
- 非上线阻塞的体验性微调

## 当前活跃阶段

- 当前进行中：`无`
- 当前活跃切片：`无（主线 9 已完成）`

## 阶段拆分

### 阶段一：认证与权限正式化

目标：

- 让 `spring-app` 成为正式认证与权限事实源
- 覆盖登录 / 注册 / me / 退出 / 角色鉴权 / 组织权限基础边界

状态：`已完成`

默认下一步：

- 已完成 `spring-app` 三类 audience（enterprise / talent / admin）认证链、PC/H5 默认登录态切换，以及管理台 `/api/admin/*` 的正式管理员权限边界

### 阶段二：主写链补缺与审计补齐

目标：

- 补齐任务、聊天、协作、验收、记录等主写链里剩余的 fallback / demo / 审计缺口
- 让写操作有稳定事务边界和审计留痕

状态：`已完成`

默认下一步：

- 已完成正式主写链第一批 gap fill：`TaskLifecycleApplicationService` 的 demo fallback 已清理，管理台写链接口已不再返回 demo 成功，PC 第一批对象页与聊天失败消费已切到“空态 + requestError”

### 阶段三：上传、IM callback 与附件回写正式化

目标：

- 让上传链、下载链、对象存储、腾讯 IM callback 验签 / 附件回写 / 异常重试进入正式链路

状态：`已完成`

默认下一步：

- 已完成 callback 正式 ack、附件回写、PC/H5 协作附件正式上传链与聊天附件正式上传会话链

### 阶段四：实时事件与可观测性升级

目标：

- 让关键页面实时状态优先走正式事件驱动
- 把日志、监控、告警、健康检查与失败追踪补到上线可用

状态：`已完成`

默认下一步：

- 已完成 `spring-app` 的正式健康检查 / readiness 视图、上传与 IM callback 失败追踪口径，以及 PC / H5 事件流状态可视化与文档同步

### 阶段五：财务、争议、风控与组织权限补缺

目标：

- 让审批、请款、开票、对账、结算、争议、风控与组织权限达到上线门槛

状态：`已完成`

默认下一步：

- 已完成后台管理前端与 fallback 对齐；当前已收口 `/api/admin/compliance`、`adminDashboard`、`adminUsers`、`adminTasks` 与 `organizationPermissions` 的后台展示

### 阶段六：承载、压测与高风险接口保护

目标：

- 补限流、防刷、幂等、压测、关键索引、慢查询治理与回滚演练

状态：`已完成`

默认下一步：

- 阶段六已完成：当前已收口登录失败限流、上传 burst guard、腾讯 IM callback 校验、发布 burst guard、`requestProtection / businessRealtimeEvent / uploadStorage / tencentImConfig` 的第一版承载细节冻结，以及部署文档 / 上线清单中的最小运行手册、回滚动作与慢查询关注点

切片执行表：

- [主线 9 剩余阶段连续执行计划](/Users/huangcongqiang/Desktop/products/youqinggong/docs/superpowers/plans/2026-03-29-go-live-standard-remaining-execution-plan.md)

### 阶段七：上线前最终验收与销项

目标：

- 逐条对照“正式上线可测”冻结条件销项
- 只在条件全部满足后，才允许把项目状态改成“正式上线可测”

状态：`已完成`

当前进度：

- 切片一 `冻结最终销项清单` 已完成
- 切片二 `按真实主路径逐项验收` 已完成：九组真实主路径、登录后整端 UI 审查、对象页来源链 / 返回链、多跳聊天 / 协作路径和在线 `health / readiness` 终验已经过门
- 切片三 `关闭剩余阻塞项` 已完成：未再发现必须上线前关闭的新阻塞项；现存前台 chunk size warning 与 root mock fallback 已降级为非阻断技术债
- 切片四 `主线 9 收口` 已完成：根 `docs/`、四个 `docs` 副本与项目总计划当前都已统一升级为 `正式上线可测`

## 完成条件

只有以下条件全部满足，本主线才允许标记完成：

1. `spring-app` 已提供正式登录 / 注册 / `me`
2. 任务主写链全部由 `spring-app + MySQL` 承接
3. 审批、请款、开票、对账、结算、争议主链都可追踪且具备审计留痕
4. 腾讯 IM 文本消息与附件回写正常
5. 上传链具备正式对象存储、下载回读与错误追踪
6. 关键页面实时状态优先事件驱动，轮询仅兜底
7. 组织权限与风控关键流程可用
8. 错误日志、健康检查、回滚路径和压测基线明确
9. 企业端 / 人才端完整主流程已在正式链路端到端跑通
10. 阶段六、阶段七各切片的整端 UI 审查与全量主路径功能回归全部通过

## 当前结论

- 主线 9 当前已完成
- 项目当前可在根文档中标记为 `正式上线可测`
- 本轮最终验收基于 `spring-app` 的正式链路完成：PC 前台、H5 前台、后台管理、上传链、IM callback、`SSE / readiness / health` 与九组真实主路径都已过门
- 本地浏览器终验使用 `spring-app` 的 H2 替代运行态完成正式链合同验证；部署与上线目标仍以 `spring-app + MySQL` 为准
- `root mock API` 继续保留为演示 / 联调 fallback，但已不再影响正式默认链路与上线测试结论

## 最后一步

- 主线 9 已完成；如后续出现新的上线阻塞项，再按项目总计划登记为新主线或主线 9 追加切片
