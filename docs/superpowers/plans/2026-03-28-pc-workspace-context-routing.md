# 2026-03-28 PC 协作空间上下文路由收口

## 目标

把 PC Web 端从聊天、记录页、工作台待办进入协作空间时的定位链路收紧，确保用户总能落到正确的任务上下文，而不是只到一个泛入口页。

## 本阶段范围

- [x] 聊天页进入协作空间时带上 `taskId / room / source`
- [x] 记录列表与记录详情进入聊天/协作空间时保留 `taskId / room / recordId / source`
- [x] 企业端工作台待办与主模块入口改成精确跳转
- [x] 人才端工作台待办与主模块入口改成精确跳转
- [x] 协作空间接收并保留 `taskId / room / source / recordId`
- [x] 根 `docs/` 与四个子项目副本文档同步本阶段结果
- [x] 运行 `frontend` 与 `frontend_mobile_h5` 构建验证

## 预期结果

- 从聊天页点“去协作空间”时，直接进入当前任务
- 从记录页/记录详情进入聊天或协作空间时，能保留来源和对象上下文
- 从工作台顶部待办进入时，能直接落到待处理对象
- 协作空间后续可以继续基于 `source` 做返回链路和提示

## 后续阶段

- PC 列表页表格化与批量操作
- 聊天 / 协作 / 记录之间的更完整返回链路
- 审批、请款、结算等办公端正式流程

## 本阶段结果

- PC Web 工作台、聊天、记录列表、记录详情进入协作空间时，已经开始统一透传 `taskId / room / recordId / source / tab`
- 协作空间已能消费并保留这些页面级上下文，聊天页切房间时也避免继续错误继承旧 `recordId`
- 根 `docs/` 与四个子项目副本文档已同步最新上下文路由口径

## 下一阶段建议

- 继续把 PC Web 的列表页收成更标准的办公端表格/筛选/批量处理结构
- 优先处理记录页与人才广场，再收通知中心和审批入口
