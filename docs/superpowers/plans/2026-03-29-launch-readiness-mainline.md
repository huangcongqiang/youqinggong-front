# `上线准备与全链回归` 主线

## 主线目标

把当前“PC / H5 可演示、`spring-app` 部分正式化”的状态，推进到“合同一致、主路径可回归、上线前检查有通过标准”的状态。

## 范围

- 首页 / 工作台 / 聊天 / 协作 / 验收 / 记录 / 通知 / 审批的主路径合同回归
- `spring-app`、PC、H5、后台管理之间的数据与状态一致性校验
- 上线前稳定性、可观测性、通过条件冻结

## 当前活跃阶段

- 当前活跃阶段：无
- 当前状态：主线 8 已完成

## 阶段拆分

### 阶段一：合同与数据回归

目标：

- 以前台真实主路径为准，验证 `spring-app` 的对象合同、返回链和状态投影
- 找出 PC / H5 / 后台管理 / `spring-app` 之间最先暴露的合同断链

状态：`已完成`

最后一步：

- 继续进入阶段二：稳定性与可观测性

### 阶段二：稳定性与可观测性

目标：

- 补齐关键监控、错误追踪、幂等追踪、回调失败告警、上传失败告警
- 确认部署、回滚、重放、补偿流程可执行

状态：`已完成`

结果：

- PC / H5 `businessEventStream` 已补状态与错误可观测钩子
- `spring-app` SSE、上传、腾讯 IM callback 已补最小结构化日志
- 前端构建、事件流测试与 `spring-app` 测试已通过

### 阶段三：上线前通过条件冻结

目标：

- 明确“正式上线可测”的唯一通过条件
- 回写版本状态总览、测试清单和后端上线前能力清单

状态：`已完成`

结果：

- “正式上线可测”的唯一通过条件已冻结到版本状态总览、测试清单和后端上线前能力清单
- 项目主线层面的已登记阶段全部收口，后续新增工作需以冻结条件为边界继续补缺
