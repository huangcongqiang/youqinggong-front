# 主线 10：PC + H5 前台全页 UI 截图评审与改版

## 目标

在不改动业务流程语义的前提下，对 `frontend` 与 `frontend_mobile_h5` 做一轮完整的前台体验升级：

- 全页截图审查
- 页面评分建档
- 跨页共性问题归纳
- 分阶段改版
- 整端回归
- 二次截图复审

当前只覆盖：

- `frontend`
- `frontend_mobile_h5`

当前不覆盖：

- `admin`
- `root mock API`
- `spring-app` 业务合同扩展

## 当前状态

- 主线状态：`进行中`
- 当前活跃阶段：`阶段五：整端 UI 复审、阻断项清零、主线收口`
- 当前活跃切片：`切片三：阶段五终轮总复审与主线 10 收口（全端分数复核 / 剩余 P1 清单清零 / 最终 docs 同步）`
- 当前默认下一步：`开始切片三的整端总复审；先基于已归档的阶段五有效图统一复核分数与剩余 P1，再决定主线 10 是否可收口`
- 当前结论：阶段一已完成，PC/H5 基线截图、评分总表与路由矩阵已建档；阶段二共享壳层与视觉基线收口已基本完成，阶段三 / 阶段四的既定待整改页面也已全部推进完首轮至三轮收口。业务页方面，PC 发布任务、PC 企业人才广场、PC 任务广场与 H5 人才任务广场、H5 发布任务当前都已完成对象页化或任务池化压缩；对象工作页方面，PC 聊天、PC 协作页、PC 验收页、PC 发单记录详情、PC 人才详情，以及 H5 聊天列表 / 详情、H5 企业协作空间、H5 验收页、H5 记录详情正常态、H5 人才详情当前都已完成一轮以上对象页收口；高频入口页方面，PC 官网首页、PC 注册页、PC 企业 / 人才工作台，以及 H5 落地页、H5 登录页、H5 注册页、H5 企业 / 人才工作台当前都已完成最终过门判断。阶段五切片一和切片二的页面都已补齐最新有效归档图，并已完成当前轮评分复核；当前正式进入终轮总复审与主线 10 收口判断

## 阶段列表

### 阶段一：截图基线与评分建档

状态：`已完成`

完成条件：

- PC 前台全页截图矩阵建立
- H5 前台全页截图矩阵建立
- 基线评分总表完成首轮建档
- 跨页共性问题与高优先级页面问题完成归纳
- 当前阶段结果同步到根 `docs/` 与四个 `docs` 副本

### 阶段二：共享视觉系统与壳层整改

状态：`已完成`

完成条件：

- PC 壳层与 H5 壳层完成统一收口
- 颜色、字体层级、间距、卡片、按钮、列表、筛选区、空态形成新的共享基线
- 第一轮共享整改后截图复核通过

### 阶段三：PC 前台逐组大改版

状态：`已完成（PC 组 1-3 页面已收口，并已切入阶段五整端复拍）`

分组：

- 组 1：门户、注册、企业/人才工作台
- 组 2：人才广场、任务广场、人才详情、发布任务、入驻
- 组 3：聊天、协作空间、验收、记录列表、记录详情、通知/审批中心

### 阶段四：H5 前台逐组大改版

状态：`已完成（H5 组 1-3 页面已收口，并已切入阶段五整端复拍）`

分组：

- 组 1：落地页、登录、注册、企业/人才工作台
- 组 2：发布、广场、详情、入驻
- 组 3：聊天列表、聊天详情、协作、验收、记录列表、记录详情

### 阶段五：整端 UI 复审、阻断项清零、主线收口

状态：`进行中`

完成条件：

- 阶段五全部截图完成
- 阶段五评分完成且复核通过
- 阶段五所有 `P0` 收口达标
- 阶段五所有 `P1` 收口达标
- 整端回归通过
- 以上条件全部满足后，主线 10 才算完成

## 固定执行规则

1. 默认使用 `superpowers`。
2. 默认尽量使用 `5` 个 agent 并行推进。
3. 截图按需抓取，立即归档，后续只读归档图，不做分批量预抓。
4. `/tmp` 只作为采集临时区，正式截图事实源以 `docs/superpowers/artifacts/...` 为准。
5. 当前线程在完全访问前提下执行本地动作时，不再重复确认权限。
6. 每个阶段至少要完成：
   - 根 `docs/` 更新
   - `frontend/docs`、`frontend_mobile_h5/docs`、`admin/docs`、`backend/docs` 同步
   - 整端 UI 审查
   - 全量主路径功能回归
7. 阶段完成后必须自动开始下一阶段，不额外停顿确认。
8. 主线 10 完成后必须自动切主线 11。
9. 阶段不过门，不允许进入下一阶段。
10. 每阶段最后一步固定是：`继续下一阶段`。

## 当前第一批问题判断

- `P0`：对象工作页首屏阻断问题当前已基本清零，后续整改进入 `P1` 收口
- `P1`：PC/H5 门户、注册与工作台已经明显收简，但标题层级、摘要密度和主入口承接还未完全统一
- `P1`：PC/H5 壳层视觉语言已开始去发光和去拟态，但共享按钮 / 标签 / 卡片语义仍未完全稳定
- `P1`：门户、工作台、业务页与对象详情页之间仍有少量首屏信息厚度、标签密度与结果反馈层级未完全消化，当前继续做整组一致性复核
- `P2`：资源池、流程页、对象详情页主要剩密度与一致性问题；聊天、协作与验收这一组以及首批业务页单页已经完成最终过门判断，高频入口页中 H5 落地页、H5 注册页、PC 企业 / 人才工作台这轮也已过门
- 当前截图判断：PC 官网首页、PC 注册页、PC 企业 / 人才工作台，以及 H5 落地页、H5 登录页、H5 注册页、H5 企业 / 人才工作台当前都已完成最新一轮截图复审取证；其中 PC 官网首页、PC 注册页、PC 企业工作台、PC 人才工作台、H5 落地页、H5 登录页、H5 注册页、H5 企业工作台与 H5 人才工作台当前已完成最终过门判断；同时，PC 发布任务、PC 企业人才广场与 H5 人才任务广场已经补齐最终过门取证，当前已正式进入第二批低分页与中心页整改

## 当前默认继续动作

1. 开始切片三的整端总复审
2. 统一复核高频页 `>= 8`、其它页 `>= 7`
3. 清理终轮仍残留的 `P1` 和文档状态漂移
4. 主线 10 完成后自动切主线 11
