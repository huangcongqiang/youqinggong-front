# 项目统一收口与增强总计划（主线 10-12）

## 目标

在保持项目对外状态为 `正式上线可测` 的前提下，把当前剩余工作统一收敛成一条连续推进链：

1. 完成 `frontend` 与 `frontend_mobile_h5` 的整端 UI 收口
2. 新增 `admin` 的全页 UI 截图评审与改版主线
3. 把上线后增强项整理成单独主线，持续推进容量、上传、IM、治理与财务审计增强

## 当前状态

- 项目状态：`正式上线可测`
- 当前活跃主线：`主线 10：PC + H5 前台全页 UI 截图评审与改版`
- 当前活跃阶段：`阶段五：整端 UI 复审、阻断项清零、主线收口`
- 当前活跃切片：`切片三：阶段五终轮总复审与主线 10 收口（全端分数复核 / 剩余 P1 清单清零 / 最终 docs 同步）`
- 已登记待执行：
  - `主线 11：Admin 全页 UI 截图评审与改版`
  - `主线 12：上线后增强项收口`
- 当前默认下一步：`继续完成主线 10 的阶段五整端 UI 复审；开始终轮总复审，统一核对分数门槛与剩余 P1，达标后判断主线 10 是否可收口`

## 主线列表

### 主线 10：PC + H5 前台全页 UI 截图评审与改版

状态：`进行中`

目标：

- 完成 PC / H5 全页截图审查、评分建档、分批改版与整端复审
- 保持主线 10 只覆盖 `frontend` 与 `frontend_mobile_h5`

当前重点：

- 完成阶段五终轮总复审
- 完成剩余 `P1` 清单清零与最终文档同步
- 达标后立即收口主线 10，并按 `10 -> 11 -> 12` 顺序继续

主线计划：

- [主线 10：PC + H5 前台全页 UI 截图评审与改版](/Users/huangcongqiang/Desktop/products/youqinggong/docs/superpowers/plans/2026-03-30-frontend-ui-audit-mainline.md)

### 主线 11：Admin 全页 UI 截图评审与改版

状态：`已登记 / 待执行`

目标：

- 对管理台 `Auth / Dashboard / Users / Tasks / Compliance` 做一轮完整截图审查、评分建档与分批改版
- 让后台页面回到“列表优先、抽屉承接、主工作区清晰”的治理端表达

主线计划：

- [主线 11：Admin 全页 UI 截图评审与改版](/Users/huangcongqiang/Desktop/products/youqinggong/docs/superpowers/plans/2026-03-30-admin-ui-audit-mainline.md)

### 主线 12：上线后增强项收口

状态：`已登记 / 待执行`

目标：

- 把当前已降级为非阻断的增强项，重新整理成持续推进主线
- 优先覆盖容量、上传/对象存储、IM 附件治理、组织权限、后台治理、财务审计与争议仲裁深化
- 主线 12 必须在阶段一、阶段二、阶段三、阶段四全部完成后才算真正收口，不允许把“完成至少第一轮增强冻结”写成阶段性完成

主线计划：

- [主线 12：上线后增强项收口](/Users/huangcongqiang/Desktop/products/youqinggong/docs/superpowers/plans/2026-03-30-post-launch-hardening-mainline.md)

## 固定执行规则

1. 项目默认推进顺序写死为：`主线 10 -> 主线 11 -> 主线 12`。
2. 当前线程对本地动作视为完全授权，不再对本地文件修改、截图抓取、归档整理等动作重复确认权限。
3. 默认使用 `superpowers`。
4. 默认尽量使用 `5` 个 agent 并行推进。
5. 截图按需抓取，完成后立即归档；`/tmp` 只作采集临时区，后续审查与实现只读取 `docs/superpowers/artifacts/...` 中的正式归档图。
6. 每个切片都必须完成：
   - 根 `docs/` 更新
   - `frontend/docs`、`frontend_mobile_h5/docs`、`admin/docs`、`backend/docs` 同步
   - 整端 UI 审查
   - 全量主路径功能回归
   - 阻断项清零
7. 每个切片、阶段与主线收口后都必须自动继续，不允许停在“后续再做”状态。
8. 每个切片最后一步固定写成：
   - `继续下一切片`
   - 或 `切到下一主线`
9. 不允许停在阶段边界。

## 主线切换顺序

1. 继续完成主线 10
2. 主线 10 收口后，立即切入主线 11
3. 主线 11 收口后，立即切入主线 12

## 完成条件

只有以下条件同时满足，才允许把这份总计划标记为“整个项目完全完成”：

1. 主线 10 收口完成
2. 主线 11 收口完成
3. 主线 12 的四个阶段全部完成收口
4. 根 `docs/` 与四个 `docs` 副本保持一致
5. 当前默认下一步始终明确，不留“后续再看”的中间态
6. 只有主线 10 / 主线 11 / 主线 12 全部收口，才允许写“整个项目完全完成”
